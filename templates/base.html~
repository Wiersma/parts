<!DOCTYPE html>
<html>
   <head>
   	<title>PARTs</title>
	<meta name="viewport" content="width=device-width, intial-scale=1.0" />
   	<link rel="shortcut icon" href="{{url_for('static', filename='favicon.ico')}}" />
   	<link rel="stylesheet" type="text/css" href="/static/style.css" />
	<link rel="stylesheet" type="text/css" href="/static/bootstrap.css" />
   	<script type="text/javascript" src="/static/jquery-1.7.1.min.js"></script>
	<script type="text/javascript" src="/static/bootstrap.js"></script>
    </head>
    <body>
	<img src="/static/foobar/banner.jpg" class="img-responsive" id="banner">
        <div class="navbar navbar-inverse navbar-static-top">
	    <div class="container">
                <a href="{{url_for('home')}}" class = "navbar-brand">PARTs</a>
                <button class="navbar-toggle" data-toggle="collapse" data-target=".navHeaderCollapse">
		   <!-- <span class="icon-bar"></span>
		    <span class="icon-bar"></span>
		    <span class="icon-bar"></span> -->
		    <span class="glyphicon glyphicon-list">
	        </button>
                <div class = "collapse navbar-collapse navHeaderCollapse">    
                    <ul class="nav navbar-nav navbar-right">
                        {% block nav %}{% endblock %}
                        <li><a href="{{url_for('home')}}">home</a></li>
                        <li><a href="/about/">about PARTs</a></li>
                        <li><a href="{{url_for('contact')}}">contact us</a></li>
                        <li><a href="{{url_for('add_entry')}}">team updates</a></li>
                        <li><a href="{{url_for('events')}}">upcoming events</a></li>
                        <li><a href="{{url_for('pictures')}}">pictures</a></li> 
                        <li><a href="{{url_for('videos')}}">videos</a></li> 
                        <li><a href="{{url_for('support')}}">donate</a></li>  
                        {% if 'username' in session %}
                        <li><a href="{{url_for('login')}}">logout</a></li> 
                        <li><a href="{{url_for('remote')}}">remote control</a></li> 
                        {% else %}
                        <li><a href="{{url_for('login')}}">login</a></li> 
                        {% endif %}
                    </ul>
                </div>
            </div>
        </div>
         <div id="content">
            {% block body %}{% endblock %}
        </div>
        <div id="flashed">
        {% with messages = get_flashed_messages() %}
        {% for message in messages %}
            {{message}}
        {% endfor %}
        {% endwith %}
        </div>
    </body>
</html>
