{% extends "base.html" %}
{% block body %}
	<h2>Pictures</h2>
    <ul>
    {% for folder in folders %}
        <li><a href="{{ url_for('display_pictures', folder=folder)}}" value = "{{folder}}">
        	{{ folder }}
        </a></li>
    {% endfor %}
    </ul>
{% endblock %}
